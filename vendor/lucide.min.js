/* Placeholder for lucide.min.js to avoid external CDN during development.
This file should contain the minified UMD build of Lucide icons.
For now, provide a minimal stub to prevent runtime errors when calling lucide.createIcons(). */
(function(root){
  var lucide = {
    createIcons: function() { /* no-op stub */ },
    icons: {}
  };
  if (typeof module !== 'undefined' && module.exports) {
    module.exports = lucide;
  } else {
    root.lucide = lucide;
  }
})(this);
